# Rust Forward Product Scope

Scope: `rust/`.
Inherits: [repository standing law](../AGENTS.md).
Owner: the tracked forward fn64 implementation and its required no-window gate.

Rust may own emulated machine truth and deterministic inspection plumbing. It
must not inherit host paths, SDL/window/audio policy, platform event loops, or
commercial/proprietary assets. Language choice is implementation material, not
fn64 product identity.

Read [the context index](../docs/INDEX.md), [current state](../docs/context/CURRENT_STATE.md),
[machine-core context](../docs/context/subsystems/machine-core.md), and the
detailed [parity ledger](PARITY.md). The required complete product gate is
`./rust/verify-forward` from repository root.

Stop on a public-authority expansion, hidden host policy, contradiction with the
parity ledger, or a behavior change outside the assigned packet. Update this
file only when the Rust workspace boundary or verification ownership changes.
