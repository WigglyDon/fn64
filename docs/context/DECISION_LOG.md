# Architectural And Workflow Decision Log

Context role: canonical accepted-decision memory.
Scope: consequential fn64 architecture and workflow decisions.
Canonical for: decision, evidence class, status, consequences, and reversal conditions.
Not canonical for: current capability detail or implementation chronology.
Inherits: [repository standing law](../../AGENTS.md).
Current-state owner: [CURRENT_STATE.md](CURRENT_STATE.md).
Related evidence: [PROJECT_HISTORY.md](PROJECT_HISTORY.md) and [rust/PARITY.md](../../rust/PARITY.md).
Update triggers: a decision is accepted, superseded, retired, or contradicted.

| ID | Decision and evidence | Reason / rejected alternative | Consequence and reversal condition | Owner | Status |
| --- | --- | --- | --- | --- | --- |
| D001 | One fact has one owner; machine state belongs to `Machine`. `USER_DECISION` | Reject hidden globals and shared host/core policy. | Every state transition needs one mutation owner. Reverse only by explicit product-law decision. | [root law](../../AGENTS.md) | current |
| D002 | Hosts own platform plumbing only. `USER_DECISION` | Reject file paths, clocks, windows, audio, or presentation inside machine stepping. | Core stays host-independent; thin hosts require real pressure. | [root law](../../AGENTS.md) | current |
| D003 | Public execution uses `Machine::step` and preserves explicit `pc` / `next_pc` cadence. `LIVE_REPO_FACT` + represented-machine capability ledger | Reject a public `Cpu::step`, generic execute dispatcher, and hidden control-flow semantics. | New execution must preserve cause/action/mutation lineage. Reverse only with a proven architecture correction. | [represented-machine capability ledger](../../rust/PARITY.md) | current |
| D004 | Proof instruments are not product/runtime truth. `USER_DECISION` | Reject boot or compatibility claims from synthetic probes. | Probe output is narrowly labeled and no-window by default. | [inspection context](subsystems/inspection-and-evidence.md) | current |
| D005 | Synthetic/generated bytes are allowed; commercial ROMs and proprietary BIOS/PIF blobs are forbidden repository/evidence content. `USER_DECISION` | Preserve lawful, reproducible proof without copyrighted payloads. | User-local assets remain outside routine inspection and packaging. | [evidence process](../process/EVIDENCE_AND_RELEASE.md) | current |
| D006 | Rust is the tracked forward implementation. `LIVE_REPO_FACT`, commit `8034b50` | Supersedes an untracked parallel sidecar and C++ product ownership. | Product work lands under `rust/`; language remains implementation material. | [current state](CURRENT_STATE.md) | current |
| D007 | `rust/verify-forward` is the sole default required product gate. `LIVE_REPO_FACT`, commit `df0551f` | Reject automatic coupling to frozen C++ comparison. | Format, clippy, tests, and both Rust probes form the forward lane. | [rust/PARITY.md](../../rust/PARITY.md) | current |
| D008 | C++ became optional frozen reference truth. `LIVE_REPO_FACT` + `USER_DECISION` | Superseded C++ as product implementation or required semantic oracle. | This transitional state ended at D017; Git history now carries the retired source. | [project history](PROJECT_HISTORY.md) | superseded |
| D009 | No-window verification is the default. `USER_DECISION` | Reject accidental SDL, product runtime, ROM, or boot execution in routine gates. | Window/audio/runtime checks require explicit authority and evidence. | [build context](subsystems/build-and-tooling.md) | current |
| D010 | Worker lanes are isolated; workers do not push, deploy, mutate canonical main, or discard unknown work. `USER_DECISION` | Keep integration authority and evidence lineage explicit. | Historical fleet law; D038 retires Worker lane execution while preserving old topology. | [packet protocol](../process/PACKET_PROTOCOL.md) | superseded |
| D011 | Transport is manual and packets are complete causal envelopes. `USER_DECISION` | Reject a fake direct-agent messaging layer and context fragments. | Every isolated boundary receives a literal self-contained packet. | [packet protocol](../process/PACKET_PROTOCOL.md) | current |
| D012 | Recursive repository context has one canonical owner per class of fact. `USER_DECISION` | Reject mutable status copied across prompts and files. | Root-to-local discovery, Context-SHA, deltas, and staleness rules govern fleet memory. | [context-system law](README.md) | current |
| D013 | Evidence labels distinguish decisions, live facts, runtime facts, claims, inference, and unknowns. `USER_DECISION` | Reject confidence as evidence. | Reports and manifests must name evidence class and exact tested source. | [context-system law](README.md) | current |
| D014 | Infrastructure is separate from product behavior. `USER_DECISION` | Reject fleet tooling as a product dependency or feature pass. | Tools remain standalone and are not wired into `rust/verify-forward` in this pass. | [fleet context](subsystems/fleet-operations.md) | current |
| D015 | Seam-090 C++ deletion was suspended. `USER_DECISION`, 2026-07-10 | Superseded the drafted but unexecuted deletion packet during the context rebase. | D017 supersedes the inventory-first and seam-090 sequences with direct Master retirement. | [project history](PROJECT_HISTORY.md) | superseded |
| D016 | Master Codex alone provisions and verifies worker branches/worktrees before supervisor or Worker packets become executable. `USER_DECISION`, 2026-07-10 | Reject supervisor-invented topology and Worker-managed branch/worktree mechanics. | Historical fleet law; D038 retires new Worker provisioning and preserves existing topology as history. | [worktree provisioning](../process/WORKTREE_PROVISIONING.md) | superseded |
| D017 | Remove the frozen C++ source/build/host/proof lane without a parity prerequisite. `USER_DECISION`, 2026-07-10 | Prefer one current product implementation and a smaller honest machine over another C++ inventory/parity pass. Rejected alternative: complete the deferred inventory lane before deletion. | Unported C++ behavior is intentionally absent, Rust remains incomplete, and Git history is the only C++ archive. A future accepted decision may independently earn behavior in Rust, but retired C++ is not restored as a product lane. | [current state](CURRENT_STATE.md), [project history](PROJECT_HISTORY.md) | current |
| D018 | Pursue an authentic cartridge boot spine using one private user-supplied ROM as runtime input. `USER_DECISION`, 2026-07-10 | Reject game-specific patches, direct-entry bypasses, fake success state, synthetic substitutes, and compatibility claims from one image. | Every earned boot checkpoint requires ROM-to-machine mutation lineage. The ROM remains untracked, unmodified, and excluded from Git/artifacts. Reverse or expand private-asset authority only by explicit product decision. | [boot lane](../lanes/real-cartridge-boot-spine-v1.md) | current |
| D019 | Clean obsolete C++-to-Rust transition scaffolding while preserving durable history and unchanged Rust product behavior. `USER_DECISION`, 2026-07-10 | Reject stale dual-language operational truth and migration machinery that no longer pays rent. | Cleanup owns non-product paths only. Moving the Cargo workspace to the repository root is deferred until the boot lane no longer owns product source and requires a separate Master decision. | [cleanup lane](../lanes/rust-purity-repo-cleanup-v1.md) | current |
| D020 | Accept BOOT-2 when one authentic ROM-derived instruction commits its complete represented effect and lineage. `USER_DECISION` + `RUNTIME_FACT`, 2026-07-10 | Reject boot claims from loading/staging alone and reject treating concrete zero storage as known PIF-produced state. | `SpecialAdd` at `0xA4000040` commits known r29+r0 into known r9 and advances cadence once. BOOT-3 remains absent; the next bounded pressure is SP IMEM plus aligned `Lw`, which requires a separate decision. | [capability ledger](../../rust/PARITY.md), [boot lane](../lanes/real-cartridge-boot-spine-v1.md) | current |
| D021 | Master GPT may initiate successive high-quality trace-driven product waves without routine seam-by-seam approval. `USER_DECISION`, 2026-07-10 | Prefer rapid evidence-backed product progress over repeated routine selection pauses; reject lane-count theater. | Product-acceleration intent remains, but D038 supersedes its supervisor/Worker fleet mechanics with direct Master execution. | [master direct workflow](../process/MASTER_DIRECT_WORKFLOW.md) | superseded |
| D022 | Product Acceleration Wave 1 uses one combined `boot-frontier-sp-imem-lw-v1` lane. `USER_DECISION` + `LIVE_REPO_FACT`, 2026-07-10 | Split rejected: SP IMEM data routing and `Lw` both change `machine.rs` data-action/application ownership, no stable target-neutral load plan exists, and authentic continuation also needs bootstrap knownness plus the boot probe. Reject a speculative interface or generic bus merely to create concurrency. | One lane owns Machine SP IMEM, narrow observed routing, complete aligned `Lw`, lineage/rollback, and bounded trace continuation. BOOT-2 remains current until accepted implementation evidence changes it. | [wave lane](../lanes/boot-frontier-sp-imem-lw-v1.md) | current |
| D023 | Defer `ordinary-control-flow-delay-slot-v1` during Wave 1. `LIVE_REPO_FACT`, 2026-07-10 | It directly overlaps the selected frontier in `machine.rs` classified action/result/application types, `cpu/instruction.rs`, `cpu/scalars.rs`, and bootstrap GPR-knownness handling. Branch-delay exception fidelity is not an independent writable seam. | No control-flow worktree or branch is provisioned. Reconsider after the frontier lane integrates and the new source boundary is re-audited; branch-likely annul remains excluded. | [CPU context](subsystems/cpu-execution.md), [wave lane](../lanes/boot-frontier-sp-imem-lw-v1.md) | current |
| D024 | Integrate `dcb9f1bf` as a truthful partial BOOT-2 hardening increment. `USER_DECISION` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-11 | SP IMEM ownership, knownness, narrow routing, and complete aligned `Lw` are valuable product truth even though the authentic load still rejects. Reject returning sound work merely for no checkpoint advance. | BOOT-2 remains highest; offset zero remains unknown; no Worker repair is issued. The creation event is not relabeled as reset, PIF, DMA, transfer, or firmware without evidence. | [capability ledger](../../rust/PARITY.md), [partial lane](../lanes/boot-frontier-sp-imem-lw-v1.md) | current |
| D025 | Wave 2 provisions only `sp-imem-bootstrap-provenance-v1`. `USER_DECISION` + `LIVE_REPO_FACT`, 2026-07-11 | Current source identified no lawful creator for SP IMEM bytes `0x000..0x003`. Ordinary control flow directly overlapped the provenance lane; ceremonial concurrency was rejected. | D026 supersedes the unknown producer state after source-qualified evidence. The no-concurrent-control-flow boundary continues through D028. | [provenance lane](../lanes/sp-imem-bootstrap-provenance-v1.md), [CPU context](subsystems/cpu-execution.md) | superseded |
| D026 | Integrate evidence-only provenance candidate `8db1b57c`. `USER_DECISION` + `LIVE_REPO_FACT` + `INFERENCE`, 2026-07-11 | Source-qualified evidence identifies retained IPL2 firmware content as the x105 SP IMEM source. Reject offset-zero hardcoding, firmware-derived profiles, or treating externally observable words as product authority. | The observed x105 path consumes `[0x000, 0x020)` and initially mutates `[0x000, 0x02c)`. No Machine behavior or checkpoint changes; BOOT-2 remains highest. | [provenance evidence](../../ops/evidence/sp-imem-bootstrap-provenance-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D027 | fn64 may accept explicitly user-supplied PIF firmware bytes as local input. `USER_DECISION`, 2026-07-11 | Authentic retained IPL2 state requires a lawful byte source. Reject bundled/downloaded/reconstructed firmware, embedded words, one-word constants, game profiles, filesystem search, and probe-authored state. | Host owns an explicit path, read/failure, and owned-byte transfer. Machine owns bytes after transfer, validation/classification, lifecycle, SP IMEM production, provenance, and rejection. Absence remains explicit; broad PIF execution is undecided. Reverse only by a later product decision. | [current state](CURRENT_STATE.md), [host context](subsystems/host-runtime.md) | current |
| D028 | Defer `ordinary-control-flow-delay-slot-v1` during the user-supplied PIF source lane. `USER_DECISION` + `LIVE_REPO_FACT`, 2026-07-11 | Firmware-source work may own `machine.rs`, `machine/cartridge_bootstrap.rs`, boot-probe plumbing, public bootstrap results, knownness, and step integration. Reject concurrent direct ownership. | D029 closes the overlapping firmware-source implementation lane. Product Wave 3 re-audits control-flow ownership independently. | [CPU context](subsystems/cpu-execution.md), [lane registry](../lanes/lane-registry.md) | superseded |
| D029 | Integrate `1fa8aa17` as `INPUT_BOUNDARY_ONLY_PRODUCT`. `USER_DECISION` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-11 | A lawful byte owner, structural validation, explicit failure states, reset persistence, and no-search host plumbing are useful product truth without guessing retained IPL2 effects. Reject treating size as authenticity or accepted bytes as known SP IMEM. | `fn64_boot_probe` accepts one optional literal `--pif-rom`; Machine owns structurally accepted immutable bytes. No PIF input was used, no SP IMEM byte is produced, and BOOT-2 remains highest. Source mapping must precede any materialization/execution choice. | [capability ledger](../../rust/PARITY.md), [input-boundary lane](../lanes/user-supplied-pif-boot-source-v1.md) | current |
| D030 | Product Wave 3 provisions `pif-ipl2-source-mapping-v1` before any PIF materialization or execution lane. `USER_DECISION` + `LIVE_REPO_FACT`, 2026-07-11 | The accepted input boundary supplies bytes but no numeric source mapping or machine-visible effect. Reject guessing offsets, variants, materialization, or an executor. | The evidence-only lane owns only `ops/evidence/pif-ipl2-source-mapping-v1/**`, uses no private input, and must classify exact mapping, variant scope, and materialization versus execution. | [mapping lane](../lanes/pif-ipl2-source-mapping-v1.md), [current state](CURRENT_STATE.md) | current |
| D031 | Product Wave 3 concurrently provisions `ordinary-control-flow-delay-slot-v1`. `USER_DECISION` + `LIVE_REPO_FACT`, 2026-07-11 | Final source audit found zero direct writable overlap: the mapping lane is evidence-only; control flow owns only registered CPU/Machine/step-probe paths plus separate evidence. Reject inventing an interface merely for concurrency. | The product lane implements `BEQ`, `BNE`, `J`, `JAL`, `JR`, and `JALR` with explicit delay-slot state, COP0 EPC/BD ownership, rollback, and synthetic proof. It receives no boot-probe or private-input authority. | [control-flow lane](../lanes/ordinary-control-flow-delay-slot-v1.md), [CPU context](subsystems/cpu-execution.md) | current |
| D032 | Integrate control-flow candidate `01b06e5a` after the mandatory PC+4 region discriminator passes. `USER_DECISION` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-12 | The shared J/JAL helper is tested across the `0xffffffff` to `0x00000000` region boundary, an exact semantic equivalent of the required `0x8fffffff` to `0x90000000` vector; separate JAL proof covers sign-extended link application. Reject waiving a high-region discriminator or inferring delay-slot fidelity from `pc`/`next_pc` alone. | `BEQ`, `BNE`, `J`, `JAL`, `JR`, and `JALR` now execute with one explicit slot, Count cadence, link/alias rules, branch-in-slot rejection, and delay-slot EPC/BD lineage. Branch-likely, REGIMM, COP0 branches, ERET, and complete MIPS control flow remain absent. BOOT-2 is unchanged. | [control-flow lane](../lanes/ordinary-control-flow-delay-slot-v1.md), [capability ledger](../../rust/PARITY.md) | current |
| D033 | Integrate mapping candidate `2ee4b3c7` as `VARIANT_SPECIFIC_MAPPING_REQUIRES_EXPLICIT_MACHINE_PROFILE`. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE`, 2026-07-12 | Pinned source and independent corroboration establish common source start `0x0d4`, NTSC end `0x71c`, and PAL/MPAL end `0x720`. The independent NTSC-like prose count conflicts with its endpoints, so endpoint arithmetic wins. Reject universalizing three pinned reconstructions or selecting a profile from structural size alone. | A later copy effect must use an explicit Machine profile. Copy materialization does not establish complete IPL2 handoff state, and minimal firmware execution remains unearned. No product behavior or checkpoint changed. | [mapping lane](../lanes/pif-ipl2-source-mapping-v1.md), [mapping evidence](../../ops/evidence/pif-ipl2-source-mapping-v1/README.md) | current |
| D034 | Product Wave 4 provisions profiled copy materialization concurrently with evidence-only handoff-state mapping. `USER_DECISION` + `LIVE_REPO_FACT`, 2026-07-12 | Final source audit found zero direct writable overlap between those historical lanes. | The profiled-copy product integrated; D038 retires the unfinished handoff Worker operation as preserved donor state. | [profiled-copy lane](../lanes/pif-ipl2-profiled-copy-materialization-v1.md), [handoff-mapping lane](../lanes/pif-ipl2-handoff-state-mapping-v1.md) | superseded |
| D035 | Integrate `a2a8ca51` as `ACCEPTED — PROFILED COPY MATERIALIZATION PRODUCT`. `USER_DECISION` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-12 | Explicit pinned profiles and lawful owned bytes now support the exact IPL1 copy effect without firmware execution or inferred identity. Reject a universal longest-copy fallback, profile autodetection, or treating independent input installation as materialization. | Machine owns NTSC/PAL/MPAL meanings, atomic full-range SP IMEM replacement, per-byte source provenance, reset persistence, and repeated-bootstrap behavior. Synthetic `Lw` proof passes; no private input, complete handoff, checkpoint advance, or compatibility fact is earned. | [profiled-copy lane](../lanes/pif-ipl2-profiled-copy-materialization-v1.md), [capability ledger](../../rust/PARITY.md) | current |
| D036 | Record the profiled-copy chat response as `WORKFLOW_DELIVERY_DEFECT`, not a product defect. `USER_DECISION`, 2026-07-12 | The chat delivery was terminal-wrapped and packet-noncompliant, while the artifact raw report, candidate tree, patch reproduction, source audit, and independent Master gates were complete. Reject representing the chat packet as compliant or wasting a semantic repair on transport alone. | Master independent evidence is the integration proof. The raw artifact report remains separately compliant; future responses must preserve one literal copy-paste packet. | [packet protocol](../process/PACKET_PROTOCOL.md), [profiled-copy lane](../lanes/pif-ipl2-profiled-copy-materialization-v1.md) | current |
| D037 | Keep `c24ab78c` unaccepted and retry `FOCUSED_REPAIR_1` after environment recovery. `USER_DECISION`, 2026-07-12 | The first repair attempt stopped before modification because tmpfs user-quota headroom was exhausted; no semantic repair occurred. | D038 retires the retry protocol. The candidate and its r31/ra defect remain preserved donor history, not accepted evidence. | [handoff lane](../lanes/pif-ipl2-handoff-state-mapping-v1.md) | superseded |
| D038 | Retire the supervisor/Worker fleet operation and use the direct Don -> Master GPT -> Master Codex -> Master GPT loop. `USER_DECISION`, 2026-07-13 | Reduce routing overhead while retaining one complete decision packet, one bounded implementation owner, independent validation, and non-destructive integration. Reject creating new supervisor seeds, Worker packets, Worker branches/worktrees, or launches. | Master Codex directly owns inspection, implementation, validation, commits, integration, authorized normal push, evidence, and reporting in one packet-bound Master worktree. Existing fleet tools and old topology remain dormant historical diagnostics; unfinished candidates remain unaccepted donors. Reversal requires an explicit workflow-law decision. | [master direct workflow](../process/MASTER_DIRECT_WORKFLOW.md) | current |
| D039 | Represent one explicit coupled cold x105 handoff for `NTSC_PINNED` only. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT`, 2026-07-13 | Pinned reconstruction plus independent NTSC-like corroboration establishes the complete retained ra link and the bounded inherited CPU state. PAL/MPAL link arithmetic remains an inference without matching independent corroboration, so universal coverage would guess. | Machine requires explicit PIF bytes, profile, x105 family, cold reset, cartridge medium, and PIF-version bit; it plans all facts before atomic application. PAL/MPAL handoff fails closed. Generated proof earns no PIF/IPL execution, BOOT-3, or compatibility claim. | [coupled-handoff evidence](../../ops/evidence/pif-ipl2-coupled-handoff-materialization-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D040 | Select cartridge-staged SP-DMEM routing for aligned `Lw` as the first generated x105 frontier. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT`, 2026-07-13 | The bounded identity order reaches an SP-DMEM `Lw` before aligned `Sw`. Decode, identity, arithmetic, AdEL, cadence, and writeback already existed; only the source-qualified SP-DMEM data target was unavailable. Reject implementing familiar `Sw` out of order or adding a generic bus/map. | Machine reuses the existing aligned-`Lw` plan and SP-DMEM storage, reads only the cartridge-bootstrap-staged span with exact source-offset provenance, and rejects unclassified backing atomically. Generated composition reaches `Sw` at `0xA4000050`; authentic BOOT-2 is unchanged. | [generated frontier evidence](../../ops/evidence/generated-x105-instruction-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D041 | Represent aligned `Sw` for direct SP IMEM only and advance the generated x105 store frontier. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-13 | The accepted generated prefix reaches `Sw` at `0xA4000050`; old r9 plus signed `0xF010` selects SP IMEM local zero and old r10 is source-known. Reject adding RDRAM/SP-DMEM/device stores, other identities, or a generic bus merely for symmetry. | Machine plans all fallible facts before four-byte mutation, records CPU-store provenance, enters AdES code 5 for unaligned access, and preserves delay-slot lineage/cadence. Thirteen generated commits reach unrepresented `RegimmBltz`; BOOT-2 is unchanged. | [aligned Sw evidence](../../ops/evidence/aligned-sw-generated-x105-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D042 | Represent only non-linking/non-likely `RegimmBltz` and advance the generated x105 branch frontier. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-13 | The accepted thirteen-step prefix reaches BLTZ on source-backed retained r31 at `0xA4000074`. Current SLT/SLTI already own one full-GPR signed comparison; discriminator values reject accidental low-word policy. Reject adding other REGIMM identities, link/annul behavior, a mode framework, or COP0 execution. | BLTZ reuses ordinary branch target, one-slot, Count, source-knownness, nested-control-flow rejection, and EPC/BD owners. Its x105 slot writes a known zero word at SP IMEM local `0x00C`; fifteen generated commits reach unrepresented `Cop0Mtc0` to Cause. BOOT-2 is unchanged. | [BLTZ evidence](../../ops/evidence/bltz-generated-x105-branch-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D043 | Represent `Cop0Mtc0` only for Cause software-pending bits, Count, and Compare on the source-backed cold-x105 path. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-14 | The generated sequence reaches consecutive MTC0 writes to registers 13, 9, and 11. Formal Cause documentation makes only IP1/IP0 writable; Count/Compare already have one committed-cadence and timer-pending owner. Reject general CP0 access, other destinations, interrupt delivery, RI, or a generic MMIO/bus layer. | Cause creates known two-bit software-pending state without clearing timer pending. Count writes before cadence. Compare clears pending before cadence and post-increment equality may relatch it. Nineteen generated commits reach the RI_SELECT `Lw` direct-target miss; BOOT-2 is unchanged. | [MTC0 evidence](../../ops/evidence/mtc0-boot-trio-generated-x105-ri-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D044 | Classify the accepted BLTZ report's branch mismatch as `MASTER_BRANCH_LABEL_OR_TOPOLOGY_DRIFT`. `USER_DECISION` + `LIVE_REPO_FACT`, 2026-07-14 | Read-only topology shows the named worktree is registered to its assigned `master/direct-bltz-x105-branch-frontier-v1` branch; only the report header used `master/direct-bltz-x105-coupled-handoff-v1`. Reject reopening accepted commits or changing preserved topology for a report-only defect. | The mismatch has no product or canonical-integration effect. The old branch and worktree remain unmodified historical state. | [current state](CURRENT_STATE.md), [project history](PROJECT_HISTORY.md) | current |
| D045 | Represent one Machine-owned cold-entry RI_SELECT zero and its exact aligned-`Lw` route. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-14 | Pinned source proves the entry read selects the cold path when zero and later writes RI_SELECT, while official NMI evidence proves RCP retention but no examined source states a generic power-on reset value. Reject deriving the load from reset kind, inventing a hardware reset table, or adding a register bank/MMIO bus. | The complete NTSC cold-x105 plan atomically creates stored RI_SELECT zero with `ColdX105Entry` provenance; construction/reset leave it unavailable. Exact physical `0x0470000C` reads through direct aliases, thirty-three generated commits reach the RI_CONFIG store miss, and RI writes/NMI/BOOT-3 remain absent. | [RI_SELECT evidence](../../ops/evidence/ri-select-generated-x105-ri-config-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D046 | Represent RI_CONFIG's defined current-control fields as one optional Machine-owned RI fact and route only its exact aligned `Sw`. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-14 | Public definitions name input bits 5:0 and enable bit 6; the bounded x105 source writes `0x40`. Reject an ignored write, raw register fabrication, undefined-high-bit guesses, calibration/timing simulation, RI_CURRENT_LOAD, and a generic RI/MMIO bus. | Exact physical `0x04700004` stores through direct aliases create input/enable fields with CPU-store lineage; undefined high bits reject before mutation. Reset and repeated cold bootstrap clear stale state. Exactly 32,035 generated commits reach the RI_CURRENT_LOAD store miss; hardware calibration, RDRAM initialization, BOOT-3, and compatibility remain absent. | [RI_CONFIG evidence](../../ops/evidence/ri-config-generated-x105-current-load-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D047 | Represent one Machine-owned RI_CURRENT_LOAD update event and route only its exact aligned `Sw`. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-14 | Public definitions name physical `0x04700008` as write-only and say any write updates current control; the bounded x105 order supplies stored RI_CONFIG before the write. Reject an ignored store, hardware-progress fabrication, RI_SELECT write, another RI register, or generic MMIO. | Exact direct aliases require known RI_CONFIG, snapshot its input/enable fields, and record low-word CPU-store evidence without output/timing state. Exactly 32,037 generated commits reach the RI_SELECT write miss; calibration, RDRAM readiness, BOOT-3, and compatibility remain absent. | [RI_CURRENT_LOAD evidence](../../ops/evidence/ri-current-load-generated-x105-ri-select-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D048 | Represent only exact x105 RI_SELECT word `0x14` as mutable Machine-owned state and route its exact aligned `Sw`. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-14 | Public definitions establish the R/W address but duplicate the receive/transmit bit range; the bounded x105 source directly establishes `0x14` and its enable-TX/RX-select purpose. Reject guessing general fields, treating the store as ignored, consulting prior RI writes as hidden authorization, implementing RI_MODE, or adding generic MMIO. | Exact direct aliases replace cold RI_SELECT value/source with `0x14` and CPU-store lineage; other words reject atomically as unsupported. Existing `Lw` observes the update. Exactly 32,038 generated commits reach the RI_MODE write miss; RDRAM initialization, BOOT-3, and compatibility remain absent. | [RI_SELECT write evidence](../../ops/evidence/ri-select-write-generated-x105-ri-mode-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D049 | Represent RI_MODE's defined low-nibble fields as one optional Machine-owned fact and route its exact aligned `Sw`. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-15 | Pinned public definitions identify operating-mode bits 1:0 and stop-transmit/stop-receive bits 2/3; bounded x105 source supplies zero and `0x0E` writes around two CPU waits. Reject physical RI effects, hidden prior-RI authorization, hardware timing, MI behavior, or generic MMIO. | Exact direct aliases store all defined low-nibble combinations with CPU-store lineage; bits above bit 3 reject atomically. The second x105 store replaces the first state/source. Exactly 32,155 generated commits execute the four- and 32-iteration CPU waits, including ORI in every second-loop delay slot, and reach the MI_INIT_MODE write miss. BOOT-2 and compatibility remain unchanged. | [RI_MODE evidence](../../ops/evidence/ri-mode-sequence-generated-x105-mi-init-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D050 | Represent only exact x105 MI_INIT_MODE command word `0x0000010F` as one optional Machine-owned MI initialization fact and route its exact aligned `Sw`. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-15 | Pinned definitions distinguish command bits from readback state, and the bounded x105 source directly supplies `MI_SET_INIT | 15`. Reject general MI commands, persisting command bit 8 as readback bit 8, MI next-write replication, RDRAM-register access, timing, or generic MMIO. | Exact direct aliases store initialization length 15 and initialization mode true with CPU-store lineage; every other low word rejects atomically. Exactly 32,158 generated commits include the MI write and delay-word construction, then reach the global RDRAM_DELAY store miss. BOOT-2 and compatibility remain unchanged. | [MI_INIT_MODE evidence](../../ops/evidence/mi-init-mode-generated-x105-rdram-delay-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D051 | Bind the exact x105 MI command to one 16-byte pending transfer and represent only its exact global RDRAM_DELAY consumer as a broadcast configuration fact. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-15 | Pinned definitions and bounded source order establish length 15, next-value repetition, global broadcast, rotated CPU word `0x18082838`, and logical fields 5/7/3/1. Reject a general burst/bus, arbitrary delay writes, per-module state, or fabricated post-transfer MI readback. | Exact direct aliases require and consume the pending 15/16 transfer, store logical configuration `0x28381808` with CPU/MI lineage, and leave current MI state unavailable. Exactly 32,159 generated commits reach global RDRAM_REF_ROW `Sw` target miss. BOOT-2 and compatibility remain unchanged. | [RDRAM_DELAY evidence](../../ops/evidence/rdram-delay-generated-x105-ref-row-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D052 | Represent only the exact global x105 RDRAM_REF_ROW zero write as a raw Machine-owned register-write fact. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-15 | Pinned definitions establish the global REF_ROW address and bounded source immediately writes zero with a broadcast comment, but establish no field layout, refresh-engine state, timing, completion, or per-module result. Reject interpreted fields, hidden RDRAM_DELAY authorization, a register bank, generic MMIO, or a bus. | Exact direct aliases accept only low word zero with known CPU-store lineage, preserve the accepted delay fact, and change no RDRAM byte. Exactly 32,161 generated commits include the REF_ROW write and following DEVICE_ID-value LUI, then reach the global RDRAM_DEVICE_ID target miss. BOOT-2 and compatibility remain unchanged. | [RDRAM_REF_ROW evidence](../../ops/evidence/rdram-ref-row-generated-x105-device-id-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D053 | Represent only the exact global x105 RDRAM_DEVICE_ID word as a Machine-owned relocation-request fact. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-16 | Pinned definitions and bounded source establish global address, word `0x80000000`, and requested base `0x02000000`, but not general fields, per-module readback, physical relocation, or address remapping. Reject hidden prior-RDRAM authorization, a register bank, generic MMIO, or a bus. | Exact direct aliases accept only the bounded word with known CPU-store lineage, preserve delay/REF_ROW facts and all bytes/routes, and record a request rather than completion. Exactly 32,176 generated commits include DEVICE_ID and fourteen setup instructions, then reach the MI_VERSION aligned-Lw target miss. BOOT-2 and compatibility remain unchanged. | [RDRAM_DEVICE_ID evidence](../../ops/evidence/rdram-device-id-generated-x105-mi-version-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D054 | Fix the current one-path Machine's immutable standard-retail MI_VERSION identity at `0x02020102` and expose one exact direct aligned read. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-17 | Pinned layout/source plus a public direct-hardware test establish the word, fields `02/01/02/02`, and x105's inequality-selected RCP 2.0 path. Reject a mutable value, alternate profile/configuration, generic MI bank, host selector, or inspection-forced branch. | Existing per-Machine `Mi` owns the one raw word; fields derive read-only. Exact KSEG aliases return it with ordinary Lw provenance. Exactly 32,183 generated commits read it, execute guest Bne and one Nop slot, select spacing `0x400` and base `0xFFFFFFFFA3F08000`, then stop at first-responder non-global RDRAM_DEVICE_ID physical `0x03F08004`. BOOT-2 and compatibility remain unchanged. | [MI_VERSION evidence](../../ops/evidence/mi-version-fixed-identity-generated-x105-first-responder-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D055 | Represent only the exact RCP 2.0 first-responder x105 RDRAM_DEVICE_ID zero write as a Machine-owned assignment-request fact. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-18 | Pinned source and generated bytes establish physical `0x03F08004`, low word zero, and InitialDeviceID lineage, but not responder presence, assignment completion, module inventory, or routing effects. Reject ranged non-global routing, RCP 1.0 support, per-module state, RDRAM_MODE, calibration, or response simulation. | Exact direct aliases accept only low word zero with known CPU-store lineage and no pending MI transfer, preserve all global RDRAM facts/bytes/routes, and record an RCP 2.0 first-responder request. Commit 32,184 performs the write; commit 32,185 constructs initial RDRAM_MODE address `0xFFFFFFFFA3F0000C`. The following JAL at `0xA40001A0` rejects before link/slot mutation because current source-backed r31 lineage cannot be replaced, so RDRAM_MODE is not reached. BOOT-2 and compatibility remain unchanged. | [first-responder evidence](../../ops/evidence/rdram-first-responder-device-id-generated-x105-cc-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D056 | Treat prior link-destination state as output state, not an ordinary-control-flow input. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-18 | JAL derives target from PC+4 and link from PC+8 without a GPR source; JALR consumes old `rs` but not unrelated old `rd`. Reject changing bootstrap lineage, weakening genuine source knownness, adding a PC/target whitelist, or building a generic provenance/control-flow framework. | JAL replaces any prior r31 value/lineage with exact instruction-result provenance. JALR retains old-source capture and alias order. At that increment, generated JAL/Nop entered InitCCValue and reached an r2 store rejection; D057 advances only that later memory frontier. RDRAM_MODE, calibration, BOOT-3, and compatibility remain absent. | [link-destination evidence](../../ops/evidence/link-destination-provenance-generated-x105-init-cc-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D057 | Permit an unavailable transfer word to commit only as one opaque aligned word owned by existing SP IMEM. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-18 | Public source establishes inherited checksum-derived r2-r5 causes but not value bits. The generated prologue writes them to an inactive aligned SP-IMEM stack range. Reject fixed register values, backing-zero truth, unknown device commands, unknown SP-DMEM, unknown arithmetic/control flow, a symbolic framework, or a second memory owner. | `SpImem` records aligned offset, instruction PC, source GPR/lineage, and addresses while canonical private zero bytes remain non-truth. Known aligned overwrite replaces opaque state; aligned Lw rejects explicitly and fetch cannot decode it. Four opaque plus twenty concrete saves reach 32,216 commits at unexecuted FindCC JAL `0xA40008F0`. BOOT-2 and compatibility remain unchanged. | [opaque SP-IMEM evidence](../../ops/evidence/opaque-sp-imem-word-generated-x105-find-cc-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
| D058 | Represent only BEQL with exact taken-slot and not-taken-annul semantics, then advance generated TestCCValue/WriteCC composition. `USER_DECISION` + `EXTERNAL_TECHNICAL_EVIDENCE` + `LIVE_REPO_FACT` + `RUNTIME_FACT`, 2026-07-19 | CPU specifications establish complete 64-bit equality, existing signed PC+4-relative targeting, one taken slot, and not-taken nullification. Reject other likely branches, unknown comparison, recursive stepping, a second delay owner, PC/path exceptions, or RDRAM_MODE implementation. | Known operands reuse ordinary control-flow planning. Taken BEQL schedules one existing slot; not-taken BEQL commits directly to P+8/P+12 with no slot fetch-as-execution, effect, exception, commit, or Count. Generated word `0x53400018` annuls `0xA40009A0`, then public stepping reaches RDRAM_MODE `Sw` word `0x46C0C0C0` at physical `0x03F0000C`, which remains an atomic `DirectTargetMiss`. BOOT-2 and compatibility remain unchanged. | [BEQL evidence](../../ops/evidence/beql-generated-x105-test-cc-frontier-v1/README.md), [capability ledger](../../rust/PARITY.md) | current |
