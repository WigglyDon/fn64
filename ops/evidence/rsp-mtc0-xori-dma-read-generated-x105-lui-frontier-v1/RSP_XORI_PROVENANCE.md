# RSP XORI Provenance

Public r3 records PC `0x01C`, exact fetched IMEM provenance, r0/value zero,
`ArchitecturalZero`, and immediate `0x0180`. `Mtc0 r3,SP_DRAM_ADDR` consumes
that exact recursive lineage. No symbolic or host-derived value exists.
