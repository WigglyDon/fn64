# Fixed identity and configuration boundary

The current product supports one fixed standard-retail NUS identity. It adds no
RCP 1.0 selector, console profile, CLI option, environment variable, filename
rule, cartridge rule, ROM rule, or test-only mutation seam.

The guest CPU comparison selects control flow. Inspection does not force the
RCP 2.0 path.
