# MI_VERSION word decision

Decision: `MI_VERSION_FIXED_STANDARD_RETAIL_NUS_IDENTITY`.

The exact immutable word is `0x02020102`. The current one-path Machine has no
alternate identity selector. `0x01010101` is only the guest comparison
constant and is not another supported fn64 identity.
