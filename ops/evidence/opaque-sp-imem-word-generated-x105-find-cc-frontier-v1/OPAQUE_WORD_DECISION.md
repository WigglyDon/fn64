# Opaque word decision

Decision: `OPAQUE_ALIGNED_SP_IMEM_WORD_ONLY`.

The committed truth is one aligned store occurrence, exact destination, exact
CPU cause, and explicit value-bit unavailability. It is not a zero word, a
symbolic expression, a checksum result, or a future load result.
