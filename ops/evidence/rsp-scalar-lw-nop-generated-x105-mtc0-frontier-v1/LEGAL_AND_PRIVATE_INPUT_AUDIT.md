# Legal And Private-Input Audit

Only repository-owned synthetic fixtures and independently public words from
the pinned x105 source were used. No private PIF image, commercial cartridge,
user-task microcode, user-derived IMEM/DMEM word, title, filename, digest, or
path was read, copied, embedded, or packaged.

The bounded proof stages the three public continuation words at `0x010`,
`0x014`, and `0x018` through an existing test-only generated-IMEM seam because
the historical compact fixture had materialized only the first four RSP words.
This is explicit proof composition, not product execution provenance or an
authentic CPU-store claim.
