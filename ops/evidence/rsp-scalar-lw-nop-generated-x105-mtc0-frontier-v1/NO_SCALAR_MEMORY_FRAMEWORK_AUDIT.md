# No Scalar-Memory Framework Audit

The candidate adds one Lw decoder branch, one immutable address/value plan,
one application path, one exact source record, and explicit rejections.

It adds no generic scalar-memory framework, unknown scalar result, symbolic
state, taint engine, bus, MMIO layer, generalized physical map, public
`step_rsp`, recursive step, scalar store, scalar J, branch, or general SLL.
