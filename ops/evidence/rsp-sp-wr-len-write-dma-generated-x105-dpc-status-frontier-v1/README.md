# RSP SP write DMA and DPC status frontier

This bounded evidence set records the exact owner and knowledge decisions for
RSP `Mtc0 r3,SP_WR_LEN`, one atomic SP-to-RDRAM transfer, the following scalar
`Xori`, and atomic rejection at `Mtc0 r3,DPC_STATUS`.

The input is the repository's deterministic public synthetic cold-x105
fixture. No private PIF image, commercial cartridge, or user-task microcode is
used.

