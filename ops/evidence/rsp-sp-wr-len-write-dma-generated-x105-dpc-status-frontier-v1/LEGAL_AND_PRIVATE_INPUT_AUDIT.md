# Legal and private-input audit

The executable gate uses deterministic bytes constructed by
`generated_pif_firmware` and independently encoded public x105 instruction
words. It reads no private PIF image, commercial cartridge, user-task
microcode, complete external source document, or another project's content.

The source anchor provenance name models how generated bytes enter IMEM; it
does not imply that a private file was opened.
