# Validation

Validation is pending the bounded implementation. The final record will name
the exact candidate SHA, Context-SHA, focused tests, format, clippy, full Rust
gate, direct probes, context/link/fleet/queue gates, clean checkout, canonical
push state, and pass-owned output cleanup.
