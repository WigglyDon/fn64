# Next frontier

The next frontier will be recorded after the generated composition has
committed `Sw` and exhausted the immediately reachable identities already
represented by `Machine::step`. No second missing instruction family is added
in this pass.
