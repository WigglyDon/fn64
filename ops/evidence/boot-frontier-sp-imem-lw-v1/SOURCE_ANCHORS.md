# Source anchors

Line numbers describe the committed candidate source shape prepared by this
lane.

- `LIVE_REPO_FACT` SP IMEM types and storage:
  `rust/crates/fn64-core/src/sp_imem.rs:6`, `:132`.
- `LIVE_REPO_FACT` Known aligned big-endian read:
  `rust/crates/fn64-core/src/sp_imem.rs:157`.
- `LIVE_REPO_FACT` Test-only Machine staging and provenance:
  `rust/crates/fn64-core/src/sp_imem.rs:217`.
- `LIVE_REPO_FACT` Concrete-zero/unknown construction:
  `rust/crates/fn64-core/src/sp_imem.rs:226`.
- `LIVE_REPO_FACT` Machine construction, reset, and public step:
  `rust/crates/fn64-core/src/machine.rs:2240`, `:2253`, `:2272`.
- `LIVE_REPO_FACT` Lw producer and applicator:
  `rust/crates/fn64-core/src/machine.rs:2283`, `:2380`.
- `LIVE_REPO_FACT` Narrow data-word classifier and SP IMEM offset rule:
  `rust/crates/fn64-core/src/machine.rs:3197`, `:3223`.
- `LIVE_REPO_FACT` Machine bootstrap creation point and GPR access lineage:
  `rust/crates/fn64-core/src/machine/cartridge_bootstrap.rs:272`, `:460`.
- `LIVE_REPO_FACT` Probe outcome presentation:
  `rust/crates/fn64-inspection/src/boot_probe.rs:438`, `:586`.
