# Raw word and requested-base boundary

Represented: CPU word `0x80000000` and requested physical base `0x02000000` from the bounded construction/comment.

Unavailable: arbitrary fields, current per-module readback, module count/presence, completed placement, and CPU-readable DEVICE_ID. Requested base is intention evidence, not completed physical truth.
