# RCP 1.0 unsupported aperture

Source spacing `0x200` would yield first-responder DEVICE_ID physical address
`0x03F04004`. Both direct aliases remain direct-target misses.

The current Machine owns only the fixed standard-retail identity and selected
RCP 2.0 guest path. This pass adds no alternate identity or RCP 1.0 route.
