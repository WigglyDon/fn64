# Validation

The evidence is accepted only with focused exact-request and generated-boundary
proofs, formatting, warnings-denied clippy, complete workspace tests,
no-window probes, `verify-forward`, context/local-link verification, fleet and
queue verification, exact-SHA clean-checkout validation, and patch
reproduction. Final counts and markers are sealed in the delivery artifact.
