# User ROM selection

The originally supplied packet named a stale singular directory. Don corrected
the local input boundary and explicitly directed this pass to the existing
plural ROM directory.

Selected basename: `oot.z64`

Source length: `33,554,432` bytes

Detected source layout: big-endian cartridge order (`.z64`)

Normalized Cartridge length: `33,554,432` bytes

The probe did not hash, patch, copy, rename, or modify the input. Neither the
private directory nor an absolute path is recorded here. Selection is a local
validation action, not product policy.
