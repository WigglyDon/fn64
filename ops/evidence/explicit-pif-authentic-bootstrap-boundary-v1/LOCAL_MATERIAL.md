# Local explicit PIF material

Classification: `UNAVAILABLE`.

The current host wiring contains only a literal optional `--pif-rom` argument.
No configured repository-local PIF reference, default, environment discovery,
or established metadata reference exists. The pass did not search for likely
filenames, enumerate binaries, ask for a path, inspect another location, or
access private firmware.

Therefore authentic x105, cartridge entry, BOOT-2, and the user-RSP pressure
ladder were not run.
