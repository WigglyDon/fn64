# No generic task-completion owner

The implementation adds no task-completion flag, callback, queue, scheduler,
host event, interrupt count, or microcode-family classifier. The represented
truth is only the exact RSP instruction commit and its existing `Sp`/`Mi`
architectural effects.

