# No cycle-accuracy claim

The seam models one atomic instruction-boundary commit. It adds no cycle
scheduler, pipeline timing, hardware stall, interrupt latency, wall clock,
host timer, or hidden service event. The ordinary successor PC policy and
CPU-next token are functional ownership decisions only.

