# Validation

The focused REF_ROW ownership, value, provenance, lifecycle, atomicity, AdES,
and generated DEVICE_ID-frontier tests pass during development. The stable
no-window step probe reports the exact REF_ROW commit and DEVICE_ID frontier
with `result: ok`. Complete candidate, clean-checkout, context, fleet, queue,
canonical, and push results are captured in the final artifact.
