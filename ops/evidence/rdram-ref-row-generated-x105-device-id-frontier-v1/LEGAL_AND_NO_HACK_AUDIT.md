# Legal and no-hack audit

Only public pinned register/source relationships and independently encoded
synthetic instruction fields were used. No private PIF firmware or commercial
ROM was searched, read, hashed, copied, staged, committed, or packaged. The
proof uses public `Machine::step`; it adds no expected-trace bypass, title,
filename, cartridge-ID, digest, generic bus, MMIO, or device registry policy.
