# Global broadcast boundary

The address is classified as the global/broadcast aperture because
`RDRAM_GLOBAL_CONFIG` adds `0x00080000` to the RDRAM base. The represented fact
does not assert installed-module count, per-module values, physical broadcast
completion, module discovery, capacity, or readiness.
